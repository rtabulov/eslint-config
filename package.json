{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "release": "lerna publish",
    "test": "yarn lint && jest"
  },
  "devDependencies": {
    "eslint": "^8.5.0",
    "jest": "^27.4.5",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "eslint-plugin-jest": "^25.3.0"
  }
}
